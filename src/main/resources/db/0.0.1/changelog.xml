<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.7.xsd">
    <preConditions>
        <dbms type="postgresql"/>
    </preConditions>

    <changeSet context="default" author="Rushan Ramazanov" id="init-extentions">
        <comment>Подключение необходимых расширений</comment>
        <sql>
            <![CDATA[
            CREATE EXTENSION IF NOT EXISTS "uuid-ossp" SCHEMA public;
            ]]>
        </sql>
    </changeSet>

    <changeSet context="default" author="Rushan Ramazanov" id="create-table-ma-medicals">
        <comment>Создание таблицы ma_medicals</comment>
        <sql>
            <![CDATA[
            CREATE TABLE ma_medicals
            (
                id                 UUID          NOT NULL DEFAULT public.uuid_generate_v4() PRIMARY KEY,
                country_en         VARCHAR(64)   NOT NULL,
                country_ru         VARCHAR(64),
                type               VARCHAR(512)  NOT NULL,
                name               VARCHAR(512)  NOT NULL,
                active_ingredient  VARCHAR(512)  NOT NULL,
                description        VARCHAR(1024) NOT NULL,
                indications        VARCHAR(2048) NOT NULL,
                contraindications  VARCHAR(2048) NOT NULL,
                dosing             VARCHAR(5096) NOT NULL,
                kidney_friendly    BOOLEAN       NOT NULL DEFAULT TRUE,
                pregnant_friendly  BOOLEAN       NOT NULL DEFAULT TRUE,
                breastfed_friendly BOOLEAN       NOT NULL DEFAULT TRUE,
                liver_friendly     BOOLEAN       NOT NULL DEFAULT TRUE,
                child_friendly     BOOLEAN       NOT NULL DEFAULT TRUE,
                stomach_friendly   BOOLEAN       NOT NULL DEFAULT TRUE
            );
            ]]>
        </sql>
        <rollback>DROP TABLE IF EXISTS ma_medicals;</rollback>
    </changeSet>

    <changeSet context="default" author="Rushan Ramazanov" id="add-indexes-to-ma-medicals-table">
        <comment>Создание индексов в ma_medicals</comment>
        <sql>
            <![CDATA[
                CREATE INDEX idx_ma_medicals_type ON ma_medicals (type);
                CREATE INDEX idx_ma_medicals_country_en ON ma_medicals (country_en);
                CREATE INDEX idx_ma_medicals_country_ru ON ma_medicals (country_ru);
            ]]>
        </sql>
        <rollback>
            DROP INDEX IF EXISTS idx_ma_medicals_type;
            DROP INDEX IF EXISTS idx_ma_medicals_country_en;
            DROP INDEX IF EXISTS idx_ma_medicals_country_ru;
        </rollback>
    </changeSet>

    <changeSet  context="default" author="Rushan Ramazanov" id="fill-medical-relations-into-ma-medicals-table">
        <comment>Заполнение записями таблицу ma_medicals</comment>
        <sqlFile path="medicals-fill-script-example.sql" relativeToChangelogFile="true"/>
        <rollback>
            TRUNCATE TABLE ma_medicals;
        </rollback>
    </changeSet>
</databaseChangeLog>